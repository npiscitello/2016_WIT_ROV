{
  "name": "wit-rov-2016-surface",
  "version": "1.0.0",
  "description": "The surface server and UI for WIT's entry in the 2016 MATE ROV competition",
  "main": "server.js",
  "scripts": {
    "build-css": "stylus source/stylesheets/default.styl -o static/css",
    "watch-css": "stylus source/stylesheets/default.styl -o static/css -w",
    "clean": "rm -rf static/css && mkdir -p static/css",
    "build": "npm run clean && npm run build-css",
    "watch": "npm run clean && npm run watch-css & nodemon server -e js,jade",
    "start": "node server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4",
    "jade": "^1.11.0",
    "morgan": "^1.7.0",
    "nodemon": "^1.9.1",
    "stylus": "^0.54.2"
  }
}
